<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tlucontactapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".StaffActivity" />
        <activity android:name=".StaffDetailActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".StudentDetailActivity" /> <!-- Thêm dòng này -->
        <activity android:name=".UnitActivity" />
        <activity android:name=".UnitDetailActivity" /> <!-- Thêm dòng này -->
    </application>
</manifest>